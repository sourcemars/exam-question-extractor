# ==================== LLM配置 ====================
# 选择LLM服务商: claude / openai
# 注意: 使用国内服务（如通义千问）时，选择 openai 并配置 OPENAI_BASE_URL
LLM_PROVIDER=claude

# ==================== API密钥 ====================
# 根据使用的provider填写对应的key

# Claude (Anthropic) - 国际服务
CLAUDE_API_KEY=your-claude-api-key-here

# OpenAI - 国际服务
OPENAI_API_KEY=your-openai-api-key-here

# ==================== 模型配置（可选）====================
CLAUDE_MODEL=claude-3-5-sonnet-20241022
OPENAI_MODEL=gpt-4o-mini

# ==================== 国内LLM服务商配置 ====================
# 通义千问（阿里云） - 推荐用于国内部署
# 使用方法：
#   1. 设置 LLM_PROVIDER=openai
#   2. 设置 OPENAI_API_KEY 为你的通义千问API密钥
#   3. 取消下面两行注释并设置正确的值
# OPENAI_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
# OPENAI_MODEL=qwen-plus  # 或 qwen-vl-plus (支持图片), qwen-max, qwen-turbo

# Kimi（月之暗面）- 适合长文档处理
# 使用方法：
#   1. 设置 LLM_PROVIDER=openai
#   2. 设置 OPENAI_API_KEY 为你的Kimi API密钥
#   3. 取消下面两行注释并设置正确的值
# OPENAI_BASE_URL=https://api.moonshot.cn/v1
# OPENAI_MODEL=moonshot-v1-8k  # 或 moonshot-v1-32k, moonshot-v1-128k

# 智谱AI（GLM-4V）- 清华背景，bbox识别能力强（推荐用于试题图片识别）
# 使用方法：
#   1. 设置 LLM_PROVIDER=openai
#   2. 设置 OPENAI_API_KEY 为你的智谱API密钥（获取地址：https://open.bigmodel.cn/）
#   3. 取消下面两行注释并设置正确的值
# OPENAI_BASE_URL=https://open.bigmodel.cn/api/paas/v4
# OPENAI_MODEL=glm-4v-flash  # 免费版本，或 glm-4v (付费版，能力更强)

# 其他兼容OpenAI格式的服务
# OPENAI_BASE_URL=https://api.custom-endpoint.com/v1

# 数据库配置
DATABASE_URL=sqlite:///./exam_questions.db

# 路径配置
PDF_DIR=data/pdfs
IMAGE_DIR=data/images
LOG_LEVEL=INFO
